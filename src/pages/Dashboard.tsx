export default function Dashboard() {
  return (
    <section aria-labelledby="today-heading">
      <h1 id="today-heading">Today</h1>
      <p className="muted">
        Today's medicines, upcoming reminders, and missed doses will appear here.
      </p>
    </section>
  );
}
